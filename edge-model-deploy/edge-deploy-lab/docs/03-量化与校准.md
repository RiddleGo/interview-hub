# 阶段 3：量化与校准

**覆盖面试题**：Q6–Q12, Q24, Q29  
**深读**：[量化可上线标准_Pro.md](../../量化可上线标准_Pro.md)、[混合精度与skip_layers_Pro.md](../../混合精度与skip_layers_Pro.md)

---

## 手算练习（Q6）

对称量化（zp=0）：

```
x = 1.5, scale = 0.1
q = round(1.5 / 0.1) = 15
x' = 15 * 0.1 = 1.5
```

试 `x = 1.56`，体会 round 误差。

---

## 校准集（Q7）

```bash
# 将 100+ 张 JPG 放入 calib/images/
python scripts/05_prepare_calib.py --config configs/project.yaml
```

要求：与 `03_preprocess.py` **完全相同** 的 letterbox、RGB、/255。

---

## PTQ 量化

**昇腾（推荐）**：

```bash
bash scripts/05_amct_quantize.sh
```

**无 AMCT 时（PC 练习 QDQ）**：

```bash
python scripts/05_ort_quantize.py --config configs/project.yaml
```

Netron 检查 QDQ 节点（Q11）。验收时比 **Dequantize 之后** 的浮点 tensor。

---

## 必做踩坑（Q12）

```bash
python scripts/03_preprocess.py --wrong-norm
python scripts/04_ort_golden.py --onnx deliverables/03_quant/model_int8.onnx
python scripts/07_compare.py
# 余弦/mAP 变差 → 改回正确预处理
python scripts/03_preprocess.py
```

---

## 分层筛查 + skip（Q24）

```bash
python scripts/08_layerwise_dump.py --config configs/project.yaml
# 余弦 < 0.98 的层写入 configs/skip_layers.txt，重跑 AMCT
```

---

## 口述要点

- **Q8**：默认 PTQ 先上车；掉点超线再 QAT / mixed precision。
- **Q29**：边缘 CNN 主流 **静态 PTQ**；动态量化不适合全图 INT8 NPU 编译。

---

## 验收标准

- [ ] `model_int8.onnx` 存在
- [ ] ORT 跑 INT8 与 FP32 关键层余弦达标（见 project.yaml thresholds）
- [ ] 预处理踩坑练习已完成并记录

---

**下一步**：[04-ATC编译与性能.md](04-ATC编译与性能.md)
